import ServicesEditor from "@/components/admin/ServicesEditor";

export default function ServicesAdmin() {
  return (
    <ServicesEditor
      page="home"
      title="Services — home section"
      description="The service cards in the 'Our services' block on the home page. The /services page now has its own separate list."
    />
  );
}
